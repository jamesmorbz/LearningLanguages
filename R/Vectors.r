fruits <- c("banana", "apple", "pineapplex")
fruits

vector <- c(1, 2, 3)
vector

numbers1 <- 1.5:6.5
numbers1

fruits <- c("banana", "apple", "pineapple", "mango", "lemon")
sort(fruit)
fruits[1]
fruits[c(1, 3)]
fruits[c(-1)]
fruits[1] <- "pear"

numbers <- seq(from = 0, to = 100, by = 20) # start, stop, step

numbers